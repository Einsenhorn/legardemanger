var module = angular.module('lgmCoreModule');

module.controller('StashItemEditController', function ($scope, $stateParams /*, StashService */) {
    var uuid = $stateParams.uuid;

    //StashService.get(uuid).then(function (item) {
    //});
});
